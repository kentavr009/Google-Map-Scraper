# .env.example

# --- Playwright Configuration ---
# Browser launch mode: 'true' for headless (no GUI), 'false' for visible.
# Default: false
HEADLESS=true

# Language for requesting reviews.
# This can affect button text, dates, etc. ('en', 'ru', 'de', ...)
# Default: en
REVIEW_LANGUAGE=en

# Browser type to use with Playwright.
# Available values: 'chromium', 'firefox', 'webkit'
# Default: chromium
BROWSER=chromium

# --- Scrolling Parameters and Limits ---
# How many rounds of scrolling without new reviews appearing should be considered "idle"
# and stop further scrolling for the current place.
# Default: 3
SCROLL_IDLE_ROUNDS=3

# Pause (in milliseconds) after each scroll step.
# Increasing this may help avoid blocks.
# Default: 1200
SCROLL_PAUSE_MS=1200

# Maximum number of attempts to open a page or fetch reviews for a single place.
# Default: 3
MAX_RETRIES_PER_PLACE=3

# Maximum number of reviews to collect from each place.
# Set '0' to collect all available reviews.
# Default: 0 (no limit)
MAX_REVIEWS_PER_PLACE=0

# Maximum number of scroll rounds in total for a single place.
# Helps prevent infinite scrolling in case of errors.
# Default: 300
MAX_SCROLL_ROUNDS=300

# --- Debugging Options ---
# Enable/disable detailed debug output to the console.
# '1' or 'true' to enable, '0' or 'false' to disable.
# Default: 1
DEBUG_SELECTORS=1

# Enable/disable the attempt to click the "Translate reviews" button.
# '1' or 'true' to enable, '0' or 'false' to disable.
# Default: 0
TRANSLATE_SWITCH=0

# Whether to filter reviews to only show English ones (if Google Maps allows it).
# This option is experimental and depends on the Google Maps UI.
# '1' or 'true' to enable, '0' or 'false' to disable.
# Default: 0
LANG_FILTER_EN=0

# --- Resource Blocking ---
# Whether to block certain types of resources (media, fonts, some trackers)
# to save traffic and speed up loading.
# '1' or 'true' to enable, '0' or 'false' to disable.
# Default: 1
BLOCK_RESOURCES=1

# --- Proxy Settings ---
# If proxies are used, a list of proxies for multi-threaded execution.
# Each proxy should be in http(s)://user:pass@host:port or socks5(h)://user:pass@host:port format.
# The `proxies.txt` file will be loaded by `main_reviews.py` if it exists.
# Example content for `proxies.txt`:
# http://user1:pass1@proxy1.example.com:8080
# socks5://user2:pass2@proxy2.example.com:1080
# http://proxy3.example.com:8080
