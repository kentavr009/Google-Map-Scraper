
# .env.example

# --- Конфигурация Playwright ---
# Режим запуска браузера: 'true' для скрытого (без GUI), 'false' для видимого.
# По умолчанию: false
HEADLESS=true

# Язык, на котором будут запрашиваться отзывы.
# Может влиять на текст кнопок, дат и т.д. ('en', 'ru', 'de', ...)
# По умолчанию: en
REVIEW_LANGUAGE=en

# Тип браузера для использования Playwright.
# Доступные значения: 'chromium', 'firefox', 'webkit'
# По умолчанию: chromium
BROWSER=chromium

# --- Параметры скроллинга и лимиты ---
# Сколько раундов скроллинга без появления новых отзывов считать "простоем"
# и прекратить дальнейший скроллинг для текущего места.
# По умолчанию: 3
SCROLL_IDLE_ROUNDS=3

# Пауза (в миллисекундах) после каждого шага скроллинга.
# Увеличение может помочь избежать блокировок.
# По умолчанию: 1200
SCROLL_PAUSE_MS=1200

# Максимальное количество попыток открыть страницу или получить отзывы для одного места.
# По умолчанию: 3
MAX_RETRIES_PER_PLACE=3

# Максимальное количество отзывов для сбора с каждого места.
# Установите '0' для сбора всех доступных отзывов.
# По умолчанию: 0 (без лимита)
MAX_REVIEWS_PER_PLACE=0

# Максимальное количество раундов скроллинга в целом для одного места.
# Помогает предотвратить бесконечный скроллинг в случае ошибок.
# По умолчанию: 300
MAX_SCROLL_ROUNDS=300

# --- Отладочные опции ---
# Включить/выключить подробный вывод отладочной информации в консоль.
# '1' или 'true' для включения, '0' или 'false' для выключения.
# По умолчанию: 1
DEBUG_SELECTORS=1

# Включить/выключить попытку кликнуть кнопку "Перевести отзывы".
# '1' или 'true' для включения, '0' или 'false' для выключения.
# По умолчанию: 0
TRANSLATE_SWITCH=0

# Фильтровать ли отзывы, чтобы показывались только на английском (если Google Maps это позволяет).
# Эта опция экспериментальна и зависит от UI Google Maps.
# '1' или 'true' для включения, '0' или 'false' для выключения.
# По умолчанию: 0
LANG_FILTER_EN=0

# --- Блокировка ресурсов ---
# Блокировать ли некоторые типы ресурсов (медиа, шрифты, некоторые трекеры)
# для экономии трафика и ускорения.
# '1' или 'true' для включения, '0' или 'false' для выключения.
# По умолчанию: 1
BLOCK_RESOURCES=1

# --- Настройки прокси ---
# Если используются прокси, список прокси для многопоточного запуска.
# Каждый прокси должен быть в формате http(s)://user:pass@host:port или socks5(h)://user:pass@host:port.
# Файл `proxies.txt` будет загружен `main_reviews.py`, если существует.
# Пример содержимого `proxies.txt`:
# http://user1:pass1@proxy1.example.com:8080
# socks5://user2:pass2@proxy2.example.com:1080
# http://proxy3.example.com:8080
